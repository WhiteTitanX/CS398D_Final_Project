chatroom message:
{
    messageID: string, //not really needed
    chatroomID: string,
    publicUserProfile: PublicUserProfile, //Who posted it
    timestamp: new Date().getTime(),
}


Chatroom:
{
    messages:[],
    name:string, //primary key (must be unique)
    icon:string,
}

User Private Profile:
{
    chatroomList:
    {
        -LVaslidhoh3qlenljasd:true
    },
    contacts:[]
}
